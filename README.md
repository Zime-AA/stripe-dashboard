# Stripe Dashboard Clone

Fullstack dashboard with:

* NestJS (GraphQL)
* PostgreSQL (Docker)
* React + Apollo

---

## Run project

### DB

docker-compose up -d

### Backend

cd backend
npm install
npm run start

### Frontend

cd frontend
npm install
npm start

---

## GraphQL

http://localhost:3000/graphql
